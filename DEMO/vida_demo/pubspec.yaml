name: vida_demo
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Inter como reemplazo de SF Pro para que la tipografía se vea igual
  # en iOS y en Web (SF Pro solo puede usarse en plataformas Apple).
  google_fonts: ^6.2.1
  dotted_border: ^3.1.0

  # Gráficas de la pantalla de Progreso. Licencia MIT.
  fl_chart: ^1.2.0
  shadcn_ui: ^0.56.2
  getwidget: ^7.0.2
  shared_preferences: ^2.5.5
  share_plus: ^13.3.0

  # Animaciones de LottieFiles. Lee archivos .lottie (que son un zip con
  # el JSON adentro) además de los .json sueltos. Licencia MIT.
  lottie: ^3.3.2

  # Varios comercios entregan su logo en SVG y no en mapa de bits.
  # Image.asset no sabe leerlos, así que sin esto esas tarjetas caían al
  # placeholder rayado. Lo usa lib/widgets/placeholder_imagen.dart.
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Datos de prueba mientras el backend no está conectado. Los lee
  # únicamente MockVidaRepository — ninguna pantalla toca estos archivos.
  assets:
    - assets/mock/
    # Animaciones Lottie. La moneda se usa en toda la app vía
    # lib/widgets/moneda_animada.dart.
    - assets/lottie/
    # El logo de la marca, que va en el header de todas las pantallas.
    - assets/img/
    # Fotos de los comercios aliados, una carpeta por filtro de Premios.
    # Flutter NO recorre subcarpetas solo: cada una va listada aparte, y
    # si mañana se agrega un filtro nuevo hay que agregar su línea acá.
    - assets/img/premios/restaurantes/
    - assets/img/premios/ropa/
    - assets/img/premios/cafecitos/
    - assets/img/premios/conveniencia/
    - assets/img/premios/deportes-ejercicio/
    - assets/img/premios/farmacias/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ============================================================
# ÍCONO Y SPLASH DE LA MARCA
#
# Los PNG de assets/marca/ NO están declarados arriba en `assets:` a
# propósito. Los leen únicamente los dos generadores de acá abajo, al
# correrlos desde la terminal; ninguna pantalla de lib/ los abre.
# Declararlos los metería dentro del bundle de la app pesando de gusto,
# porque el ícono real que usa el teléfono es el que estos generadores
# escriben en ios/ y android/, no el PNG original.
#
# Los dos se corren a mano y dejan archivos versionados:
#   dart run flutter_launcher_icons
#   dart run flutter_native_splash:create
# ============================================================

flutter_launcher_icons:
  ios: true
  android: true
  image_path: "assets/marca/icono-masvida-1024.png"

  # App Store Connect RECHAZA un ícono que traiga canal alfa, y el error
  # no aparece hasta que se sube el build. Esto le aplana la
  # transparencia antes de que eso pase.
  remove_alpha_ios: true

  # Android dibuja el ícono en dos capas y les aplica la máscara del
  # launcher (círculo, redondeado, etc.), así que la cruz va suelta y
  # transparente sobre un fondo blanco puesto por separado.
  adaptive_icon_foreground: "assets/marca/icono-masvida-android-foreground.png"
  adaptive_icon_background: "#FFFFFF"

flutter_native_splash:
  # El MISMO color que pinta PantallaCargando (AppColors.background).
  # El splash nativo entrega la pantalla al primer frame de Flutter: si
  # los dos fondos no son idénticos, en ese cambio se ve un parpadeo de
  # color.
  color: "#F5F6FA"
  image: "assets/marca/splash-cruz.png"

  # Android 12 en adelante ignora la configuración de arriba y usa su
  # propio splash del sistema, así que hay que repetírselo acá.
  android_12:
    color: "#F5F6FA"
    image: "assets/marca/splash-cruz.png"
